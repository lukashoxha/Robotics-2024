public class Action {
    public static void main(String[] args) {
        // Create an object of the Person class
        Person person = new Person();

        // Print the age
        person.printAge();

        // Make the age older
        person.makeOlder();

        // Print the age again
        person.printAge();
    }
}
